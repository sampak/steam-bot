export default {
  steam: {
    accountName: process.env.account_name ?? 'xxx',
    password: process.env.password ?? 'xxx',
    sharedSecret: process.env.shared_secret ?? 'xxx', 
  },
  apikey: process.env.apikey ?? 'xxx'
}